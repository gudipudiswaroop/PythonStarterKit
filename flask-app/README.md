# This module shall cover a sample hello world webapp implementation with flask framework
# This should cover unit test cases
# pip install psycopg2, psycopg2 is an adapter to connect to postgresql DB